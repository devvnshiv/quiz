import 'package:flutter/material.dart';

class loaderror extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text(" OOPS Error try again !!!")),
    );
  }
}
